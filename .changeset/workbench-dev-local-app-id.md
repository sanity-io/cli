---
"@sanity/workbench-cli": patch
"@sanity/cli": patch
---

Identify a locally-served workbench app by its `<host>-<port>` instead of its deployment app id, so a running dev app never collides with its deployed twin.
