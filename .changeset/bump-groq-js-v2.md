---
'@sanity/cli': patch
---

Bump `groq-js` to `^2.0.0` and satellite packages that already use it (`@sanity/migrate@^8.0.1`, `@sanity/runtime-cli@^17.3.0`). `@sanity/codegen` stays on `^7` for now.
