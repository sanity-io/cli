---
"@sanity/cli": minor
---

Adds `sanity api <endpoint>` to call any operation listed by `sanity api list`. Supports body construction via `-f`/`-F`/`--input`, query and header flags, dry-run, streaming, and an interactive guard for destructive methods (PATCH/PUT/DELETE).
