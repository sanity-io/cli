---
"@sanity/cli": minor
---

Adds `sanity api <endpoint>` to call any operation listed by `sanity api list`. Supports `-X`, `-q`, `--project`, `--dataset`, `--token`, `--json`, and `--yes`. Destructive methods (PATCH/PUT/DELETE) require `--yes` in unattended contexts.
