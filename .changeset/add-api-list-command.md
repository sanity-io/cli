---
"@sanity/cli": minor
---

Add `sanity api list` for listing every operation in Sanity's HTTP API, with `--spec`, `--method`, `--capability`, and `--grep` filters; deprecates `sanity openapi list`.
