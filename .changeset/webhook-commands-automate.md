---
'@sanity/cli': minor
---

Make webhook commands safe to automate by printing the creation URL instead of opening a browser,
requiring a webhook name when deletion or log selection would otherwise prompt, and returning usage
errors without starting interactive work.
