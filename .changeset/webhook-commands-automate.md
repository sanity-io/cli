---
'@sanity/cli': minor
---

feat(cli): support unattended mode for hook commands

Print webhook creation URLs instead of opening a browser, and require names when deletion or log
selection would otherwise prompt.
