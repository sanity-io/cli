---
"@sanity/workbench-cli": patch
---

Emit a standalone SPA (`index.html` + bootstrap + favicons) alongside the module-federation remote when building a workbench remote (`SANITY_INTERNAL_IS_WORKBENCH_REMOTE`)
