---
'@sanity/cli': patch
---

Sanitize `unstable_defineApp` icons through the shared manifest allowlist (the same DOMPurify config the studio manifest uses) before inlining them in the core-app manifest.
