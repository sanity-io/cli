---
"@sanity/cli": patch
"@sanity/workbench-cli": patch
---

Pre-fill the `slug` in the scaffolded workbench `sanity.cli.ts` (`sanity init --unstable--workbench`): studios default it from the entered project name, apps from the output directory name.
