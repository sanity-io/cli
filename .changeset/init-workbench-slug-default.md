---
"@sanity/cli": patch
"@sanity/workbench-cli": patch
---

Pre-fill the `slug` in the scaffolded workbench `sanity.cli.ts` (`sanity init --unstable--workbench`), defaulted from the entered name/title.
