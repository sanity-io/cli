---
'@sanity/cli': minor
---

Configure agent skills as part of `sanity init`, and add the `sanity skills add` and `sanity skills update` commands for installing and refreshing Sanity agent skills in existing projects
